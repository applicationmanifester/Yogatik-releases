// Stub for Service Worker registration (no-op in dev)
export function registerSW() {
  // In production this would register a service worker.
  // For local dev we simply do nothing to avoid missing module errors.
}
